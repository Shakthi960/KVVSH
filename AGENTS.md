# AGENTS.md

## Git workflow

- After completing any code changes, commit them with a concise message in the repo's existing style.
- Always push changes to `origin/main` after committing.
- Never commit secrets or environment values (e.g. MYSQL_PASSWORD, MAIL_PASSWORD).
